apiVersion: v1
kind: Service
metadata:
  name: oldivrblast
  namespace: connecthub
spec:
  type: ClusterIP   # Change to ClusterIP or NodePort as needed
  ports:
    - port: 80          # Port to expose
      targetPort: 80  # Port your application listens on
  selector:
    app: oldivrblast
