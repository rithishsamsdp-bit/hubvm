services:
  conferenceconsumer:
    build:
      context: ../..
      dockerfile: docker/confrenceconsumer/Dockerfile
    container_name: conferenceconsumer
    working_dir: /app
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - connecthub_network
    volumes:
      - ../../confrenceconsumer/app:/app

networks:
  connecthub_network:
    driver: bridge