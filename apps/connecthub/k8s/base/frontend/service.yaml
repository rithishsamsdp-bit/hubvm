apiVersion: v1
kind: Service
metadata:
  name: connecthub-frontend
  namespace: connecthub
spec:
  type: ClusterIP  # Change to NodePort or LoadBalancer if needed
  ports:
    - port: 80         # Port to expose
      targetPort: 80   # Port your application listens on
  selector:
    app: connecthub-frontend
